CSC309 Lecture 7

February 24, 2020

const courseEnrollment = students.reduce((enrollment, student) => {
	student.courses.map((course) => {
		if (course in enrollement) {
			enrollment[course] = enrollment[course] + 1
		} else {
			enrollment[course] = 1
		}
	})
	return enrollment
}, {})